Compare the specific products named in $ARGUMENTS.

For each product, find its pricing across all stores in `catalogs/` and `products.json`. Show a side-by-side comparison of: wattage, battery life, waterproof rating, weight, price at each store, international RRP, and RRP deviation percentage.

Output a concise comparison table to the terminal. If the user asks, also generate a Typst PDF.